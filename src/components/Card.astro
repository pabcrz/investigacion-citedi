---
import { cards } from "../lib/data";
---

<div class="w-max-content flex flex-wrap gap-8 justify-center">
  {
    cards.map((card, i) => (
      <div
        class={`p-4 w-80 h-80 rounded-sm`}
        style={{
          borderColor: card.color, // Asignar el color al borde
          backgroundColor: card.color, // Asignar el color como fondo
        }}
      >
        <div class="flex flex-col justify-evenly h-full text-white">
          {card.items.map((item, i) => (
            <div class="flex justify-between gap-4 items-center">
              <p
                class={`rounded-full min-w-20 size-20 border-2 flex items-center justify-center text-white text-wrap text-4xl font-bold`}
                style={{
                  backgroundColor: card.color, // Asignar el color como fondo
                }}
              >
                {item.numbers}
              </p>
              <p class="w-auto text-sm" set:html={item.content} />
            </div>
          ))}
        </div>
      </div>
    ))
  }
</div>
